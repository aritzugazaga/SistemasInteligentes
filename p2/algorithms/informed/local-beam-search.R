local.beam.search = function(problem,
                             max_iterations = 100,
                             count_print = 100,
                             trace = FALSE,
                             beams,
                             filename){
  
  
}